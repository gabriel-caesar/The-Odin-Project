<nav class='navbar flexcenter'>
  <ul style='display:flex;'>
    <% for (let i = 0; i < links.length; i++) { %>
      <li>
        <a href="<%= links[i].href %>" style='margin-right: 2rem; outline: none; color: #fff;'>
          <span><%= links[i].text %></span>
        </a>
      </li>
    <% } %>
  </ul>
</nav>